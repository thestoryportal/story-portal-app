Read the prompt from `tools/ai/inbox/latest.md` and implement the requested changes.

After implementing:

1. Run `pnpm lint` to ensure code quality
2. Report what files were changed
3. Explain how to test the changes
