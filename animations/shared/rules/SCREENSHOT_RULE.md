# Screenshot Rule

After any UI-visible change, run:
```bash
node animations/shared/capture/run.mjs --mode smoke --label <description>
```

Then report:
- the new screenshot folder path
- what changed visually
